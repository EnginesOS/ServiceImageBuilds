[core]
				        repositoryformatversion = 0
				        filemode = true
				        bare = false
				        logallrefupdates = true
				[branch "$release"]
				[remote "origin"]
				        url = https://github.com/lachdoug/first_run_gui.git
				        fetch = +refs/heads/*:refs/remotes/origin/*
				[branch "$release"]
				        remote = origin
				        merge = refs/heads/$release
				